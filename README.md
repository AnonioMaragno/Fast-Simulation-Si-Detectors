# Fast-Simulation-Si-Detectors
## Lo scopo di questo programma è simulare un esperimento ad un collider che studia collisioni p-p tramite rivelatori di vertice per determinare la coordinata z del vertice primario

Per compilare il progetto è sufficiente eseguire la macro "compilaMacro.C"
